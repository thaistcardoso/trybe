const notaCandidato = 72;

if (notaCandidato >= 80) {
    console.log("Parabéns, você foi aprovado!");
} else if (notaCandidato < 80 && notaCandidato >= 60) {
    console.log("Parabéns, você está na nossa lista de espera!");
} else {
    console.log("Você foi reprovada(o)!");
};
